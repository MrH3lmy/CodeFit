package com.codefit.model;

/**
 * Whether a piece of learning material an {@link InterviewRequirement} points at already exists in
 * CodeFit (a deck/module or an existing subsystem) or is planned for a later slice.
 */
public enum InterviewRequirementStatus {
    AVAILABLE,
    PLANNED
}
